using UnityEngine;

/**
 * The purpose of this component is to make hierarchy selection easier in cases where there are many
 * child objects that are easily but inadvertently selectable. Place this component on a parent object
 * and it will always be selected first by default.
 */
[SelectionBase]
public class SelectionBase : MonoBehaviour
{
}